# SUN-1222C — CDP existing-credential fresh mainnet authorization

**Checkpoint:** `SUN-1222C-CDP-EXISTING-CREDENTIAL-FRESH-MAINNET-AUTHORIZATION`
**Type:** Human governance decision (risk acceptance), documentation-only. No deployment, secret, or D1 mutation.

## 1. Decision

The human release authority for SITEBORNE has issued a fresh, direct authorization superseding the prior recommendation from `SUN-1222C-CDP-MAINNET-CREDENTIAL-PROVENANCE-CLOSURE` (`docs/reports/SUN-1222C-cdp-mainnet-credential-provenance-closure.md`).

| Field | Prior state | New state |
|---|---|---|
| `CDP_CREDENTIAL_DISPOSITION_RECOMMENDATION` | `ROTATE_TO_NEW_EXPLICITLY_GOVERNED_CDP_KEY` | `KEEP_CURRENT_KEY_AND_FRESHLY_AUTHORIZE` |
| `FRESH_MAINNET_CREDENTIAL_AUTHORIZATION_READY` | `NO` | `FRESH_MAINNET_CREDENTIAL_AUTHORIZATION_ISSUED=YES` |
| `CDP_MAINNET_CREDENTIAL_GOVERNANCE_BLOCKER` | `OPEN` | `CLOSED` (by human authorization, not by independent technical proof — see §4) |

The prior rotation recommendation was based on an inability to *independently reconstruct* historical Cloudflare secret-installation provenance (audit log read returned 403; no fingerprint match available). It was **not** based on any evidence of compromise, disclosure, unauthorized use, malfunction, or ownership dispute. The credential owner has reviewed that gap and elected to accept it rather than rotate.

## 2. Authorization scope

Credential: `CDP_API_KEY_ID` / `CDP_API_KEY_SECRET`, historically labeled `siteborne-x402-facilitator`.

Authorized for: SITEBORNE production x402 facilitator authentication.

- Network: Base mainnet, `eip155:8453`
- Asset: USDC, `0x833589fCD6EDb6E08f4c7C32D4f71b54bdA02913`
- Governed seller: `0x7f44a2dd237938F18632d4CcA40f4c690295E6E1`
- Authorized service family: `verify_agent_output.v2`, `web_context_verified.v2`, and future services only when separately activated through their own governed release checkpoints.
- Explicitly **not** activated by this authorization: `company_evidence_graph.v2`, `document_evidence_json.v2`, `document-artifact-upload`.

## 3. Read-only binding confirmation

Verified live against Cloudflare (names only, no secret values read):

```
$ wrangler secret list --name siteborne-utility-edge
CDP_API_KEY_ID       secret_text   present
CDP_API_KEY_SECRET   secret_text   present
```

Source trace (unchanged from prior checkpoint): both `VERIFY_V2_CDP_ROUTE` and `WEB_CONTEXT_V2_CDP_ROUTE` bind to this same pair. `NVM_ENVIRONMENT=sandbox` is a separate Nevermined configuration value and is not evidence bearing on this CDP credential's network.

```
VERIFY_ROUTE_CDP_BINDING=CDP_API_KEY_ID/CDP_API_KEY_SECRET
WEB_ROUTE_CDP_BINDING=CDP_API_KEY_ID/CDP_API_KEY_SECRET
NVM_ENVIRONMENT_RELEVANT_TO_CDP_ROUTES=NO
CURRENT_CDP_BINDING_NAMES_PRESENT=YES
VERIFY_ROUTE_USES_AUTHORIZED_BINDING=YES
WEB_ROUTE_USES_AUTHORIZED_BINDING=YES
```

## 4. Human attestation (not independently verified)

The following are recorded as the credential owner's direct attestations. They are governance evidence of a risk-acceptance decision, not independently derived technical proof — Cloudflare audit-log access remains unavailable (403) and no live CDP account/API check was performed against the secret value in this or any prior checkpoint.

```
CREDENTIAL_NEVER_SHARED=YES                 (HUMAN_ATTESTATION)
CREDENTIAL_KNOWN_COMPROMISED=NO             (HUMAN_ATTESTATION)
CREDENTIAL_TESTED_WORKING=YES               (HUMAN_ATTESTATION)
CREDENTIAL_OWNER_DIRECTS_CONTINUED_USE=YES  (HUMAN_ATTESTATION)
```

## 5. SUN-1219 historical authority (corrected pointer)

Current-branch authority for the SUN-1219A attestation record is `ac0fb6f7b55fd68b0b6047066450f27f66ea45a1` (verified reachable from `main`; content-identical to the previously-cited stale backup ref `493afbd`, which predates a history rewrite and is not itself an ancestor of `main`). This new authorization does not depend on reconstructing that historical chain to establish present-day human consent — the consent recorded in this document is a fresh, direct statement made in this checkpoint.

## 6. No secret rotation performed

No CDP credential was created, rotated, deleted, revoked, exposed, or reinstalled. `CDP_SECRET_MUTATIONS=0`.

## 7. Containment unchanged

This authorization does not re-enable paid routes. Verified live:

```
ACTIVE PUBLIC DEPLOYMENT: d28f30c5-b83c-42a8-b0e4-3f3a7c2bc7d1 @ 100% (sole member)
PAID_ROUTES_ENABLED=false
b6b7477f: retained immutable version, not an active deployment member
Model-C normal (0fd6d9bd) / quiescence (afe08ea7): unassigned
PAID_ADMISSION_CLOSED=YES
```

## 8. Prior economic evidence (unchanged, not re-derived here)

`KNOWN_CONFIRMED_BASE_MAINNET_SETTLEMENTS=2` (both previously classified as authorized release-test transactions). `COMPANY_EIGHT_SETTLEMENTS=0`. No new transaction was generated by this checkpoint.

## 9. Decision record

```
HISTORICAL_CREDENTIAL_INSTALLATION_PROVENANCE=INCOMPLETE
CURRENT_HUMAN_CREDENTIAL_AUTHORIZATION=PROVEN_BY_FRESH_DIRECT_AUTHORIZATION
CREDENTIAL_COMPROMISE_EVIDENCE=NONE_KNOWN
CREDENTIAL_ROTATION_REQUIRED=NO
CDP_CREDENTIAL_DISPOSITION=KEEP_CURRENT_KEY_AND_FRESHLY_AUTHORIZE
CDP_MAINNET_CREDENTIAL_GOVERNANCE_BLOCKER=CLOSED
```

This closes the governance/provenance blocker only. It does not itself prove future runtime acceptance — that remains subject to the next governed release checkpoint (`SUN-1222C-PCC-MODEL-C-QUIESCENCE-ONLY-PUBLIC-CUTOVER`), which does not re-enable paid routes either.

## 10. Production invariance

```
PUBLIC_DEPLOYMENT_MUTATIONS=0
PUBLIC_VERSION_UPLOADS=0
PAID_VERSION_UPLOADS=0
D1_WRITES=0
SECRET_MUTATIONS=0
CDP_KEY_CREATIONS=0
CDP_KEY_DELETIONS=0
PAYMENT_AUTHORIZATIONS_SUBMITTED=0
FACILITATOR_VERIFY_CALLS=0
FACILITATOR_SETTLE_CALLS=0
PROVIDER_EXECUTIONS=0
WORKFLOW_CREATIONS=0
SETTLEMENTS=0
CHAIN_TRANSACTIONS=0
ECONOMIC_EFFECT_USDC=0
```
