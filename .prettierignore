# Mirrors .gitignore for the categories that trip up `prettier --check .`
# when run after Python/Vitest/pnpm produce local generated artifacts.
# Keeps root `pnpm format:check` (and therefore `pnpm check`) green
# regardless of what generated caches happen to exist locally.

node_modules
.pnpm-store
dist
build
*.tsbuildinfo
coverage
.nyc_output
.claude

# Python
__pycache__
*.pyc
*.pyo
*.pyd
.pytest_cache
.venv
.mypy_cache
.ruff_cache

# Lockfiles (auto-generated; not hand-formatted)
pnpm-lock.yaml

# SUN-1200 checkpoint F: machine-generated AJV standalone validator
# module -- minified codegen output, never hand-edited, and its exact
# byte content is compared verbatim against
# generate-input-validators.mts's own output by
# generate-input-validators:check (schemas:check). Reformatting it would
# make that drift check meaningless.
apps/edge-api/src/generated/input-validators.generated.js
apps/edge-api/src/generated/output-validators.generated.js
